<% if @shows.empty? %>
  <p><%= t 'shows.search.no_shows_found' %></p>
<% else %>
  <div class="pull-left">
    <div class="btn-group">
      <a href="/shows/search?<%= @query.gsub(/&status=(Continuing|Ended)/, '') %>" class="btn btn-primary btn-hover btn-xs">All</a>
      <a href="/shows/search?status=Continuing&<%= @query %>" class="btn btn-primary btn-hover btn-xs">Continuing</a>
      <a href="/shows/search?status=Ended&<%= @query %>" class="btn btn-primary btn-hover btn-xs">Ended</a>
    </div>
  </div>
  <div class="btn-group pull-right">
    <a href="/shows/?<%= @query %>" class="btn btn-default btn-hover btn-primary btn-xs"><%= glyphicon 'th-large' %></a>
    <a class="btn btn-default btn-primary btn-xs active"><%= glyphicon 'th-list' %></a>
  </div>
  <% @shows.each do |group, shows| %>
    <section>
      <h6 class='list-header'><%= group %></h6>
    <% shows.each do |show| %>
      <a href="/shows/<%= show.id %>">
        <div class="card small">
          <% unless show.poster.empty? %>
            <%= image "http://thetvdb.com/banners/#{show.poster}" %>
          <% end %>
          <div class="content"><h1><%= show.name %></h1></div>
        </div>
      </a>
    <% end %>
    </section>
  <% end %>

  <%= partial :'partials/paging', locals: { url: @url } %>
<% end %>
