#!/usr/bin/env ruby

require 'sequel'
require 'awesome_print'
require 'pry'

DB = Sequel.postgres('showtracker',
                     host: 'localhost',
                     user: 'showtracker',
                     password: 'showtracker')

Dir['./{models}/**/*.rb'].each { |file| require file }

Pry.start
